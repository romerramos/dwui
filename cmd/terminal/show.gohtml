<div 
    id="terminal" 
    class="w-full h-full" 
    data-container-id="{{.ContainerID}}"
    x-data="terminal('{{.ContainerID}}')"
    x-init="initTerminal()"
    x-ref="terminalElement">
</div>
    
